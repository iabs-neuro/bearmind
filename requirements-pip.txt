# BEARMiND pip dependencies (MINIMAL)
# Install after conda environment: pip install -r requirements-pip.txt
#
# Only packages ACTUALLY USED in the codebase
# Dependencies are auto-installed by pip

# === VERSION LOCK (prevent pip from upgrading conda packages) ===
numpy<2.0  # TensorFlow 2.10 and CaImAn require NumPy 1.x

# === CORE NEUROSCIENCE ===
driada>=0.6.0

# === MACHINE LEARNING ===
interpret>=0.5
sortedcontainers>=2.4.0

# === IMAGE/VIDEO ===
opencv-python>=4.8
moviepy>=1.0.3,<2.0
