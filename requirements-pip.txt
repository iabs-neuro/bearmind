# BEARMiND pip dependencies (MINIMAL)
# Install after conda environment: pip install -r requirements-pip.txt
#
# Only packages ACTUALLY USED in the codebase
# Dependencies are auto-installed by pip

# === CRITICAL: NumPy constraint ===
# This prevents pip from upgrading conda's numpy (causes DLL conflicts on Windows)
# We don't install numpy via pip - conda handles it - but we constrain it
numpy<2.0

# === CORE NEUROSCIENCE ===
driada>=0.6.0

# === MACHINE LEARNING ===
interpret>=0.5
sortedcontainers>=2.4.0

# === IMAGE/VIDEO ===
opencv-python>=4.8
moviepy>=1.0.3,<2.0
